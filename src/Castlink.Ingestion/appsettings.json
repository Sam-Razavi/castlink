{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "Postgres": ""
  },
  "Tmdb": {
    "ApiKey": "",
    "_comment": "Never set ApiKey here. Use `dotnet user-secrets set Tmdb:ApiKey <key>` locally, or the Tmdb__ApiKey app setting in Azure."
  },
  "Ingestion": {
    "Mode": "Incremental",
    "_modeComment": "'FullSeed' or 'Incremental'. Defaults to Incremental — FullSeed is a deliberate, expensive one-time action, opt in explicitly.",
    "FullSeedStartYear": 1970
  }
}
